# Packages required for building and testing libfuse only,
# no python code is required when using libfuse.
# Build packages: 
meson
ninja
# Test packages: the suite itself is standard library only.
codechecker
